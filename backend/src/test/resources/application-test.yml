spring:
  datasource:
    url: jdbc:h2:mem:community-test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

app:
  jwt:
    secret: SevenKnightsCommunityTestSecretKeyMustBe32CharsOrMore!!
    expiration-ms: 86400000
  cors:
    allowed-origins: http://localhost:3000
  upload:
    dir: uploads-test
